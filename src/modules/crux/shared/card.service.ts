// placeholder — CRUX Card Service (NOT an agent — pure rendering)
// Assembles card data from score + property profile
// Generates OG-tag shareable URL: crux.comfhutt.com/card/[share_token]
// Personalized for signed-in users (intent profile label + intent-specific score)
// No Puppeteer, no image generation in MVP — share URL IS the card
// WhatsApp/Twitter/LinkedIn auto-render via Open Graph meta tags
